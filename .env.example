# Backend
# Toggle to use OpenAI embeddings instead of local sentence-transformers
USE_OPENAI=false
OPENAI_API_KEY=sk-...

# Frontend (optional)
# FRONTEND_ORIGIN can be used to set CORS dynamically if you extend the backend
FRONTEND_ORIGIN=http://localhost:3000
